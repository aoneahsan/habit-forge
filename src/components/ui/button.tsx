// Re-export Radix UI Button to maintain compatibility
export { Button, type ButtonProps } from '@radix-ui/themes';

// For backward compatibility with variant prop
export const buttonVariants = () => '';